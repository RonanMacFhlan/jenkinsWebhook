pipeline {
    agent { label 'page-size-jenkins-agent' }
    stages {
        stage('execute pipelint') {
            steps {
                sh "echo 4 change worked"
            }
        }
    }
}
